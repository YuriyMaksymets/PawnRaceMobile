<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PawnRaceMobile.Screens.ColorSelectionPage">
    <ContentPage.Content>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="5*" />
            </Grid.RowDefinitions>
            <ContentView x:Name="onePlayerButton" BackgroundColor="{StaticResource dark}"
                     Grid.Row="0">
                <Label Text="White" Style="{StaticResource lightLabel}" />
            </ContentView>

            <ContentView BackgroundColor="{StaticResource light}"
                     Grid.Row="1">
                <Label Text="Select your color" Style="{StaticResource descriptionLabel}" />
            </ContentView>

            <ContentView x:Name="twoPlayersButton" BackgroundColor="{StaticResource dark}"
                     Grid.Row="2">
                <Label Text="Black" Style="{StaticResource lightLabel}" />
            </ContentView>
        </Grid>
    </ContentPage.Content>
</ContentPage>